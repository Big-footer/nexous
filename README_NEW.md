# NEXOUS

## AI Agent Platform - 에이전트를 만들어내는 시스템

> **NEXOUS는 AI 에이전트가 아닙니다.**  
> **NEXOUS는 AI 에이전트를 정의, 생성, 실행, 추적하는 플랫폼입니다.**

---

## 🎯 핵심 원칙

### NEXOUS가 하는 것 (플랫폼 책임)
- ✅ **Agent Lifecycle 관리** - 에이전트 생명주기 추적
- ✅ **Memory / Trace 관리** - 실행 기록 저장 및 추적
- ✅ **LLM / Tool 추상화** - 다양한 LLM과 도구 통합
- ✅ **실행 파이프라인 제공** - 오케스트레이션

### NEXOUS가 하지 않는 것 (에이전트 책임)
- ❌ 분석하지 않습니다
- ❌ 글을 쓰지 않습니다
- ❌ 판단을 내리지 않습니다
- ❌ 도메인 지식을 갖지 않습니다

**위 작업은 NEXOUS가 생성한 ProjectAgent의 책임입니다.**

---

## 🏗️ 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                    NEXOUS PLATFORM                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   DEFINE     │  │   CREATE     │  │    RUN       │          │
│  │  Registry    │→ │   Factory    │→ │ Orchestrator │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│         │                                    │                  │
│         └──────────→ TRACK/AUDIT ←──────────┘                  │
│                      (Trace)                                    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ 생성
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│              생성된 AI 에이전트 인스턴스들                        │
│        (실제 작업 수행 - 분석, 글쓰기, 판단, 도메인 지식)          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📁 프로젝트 구조

```
NEXOUS/
├── nexous/                    # 플랫폼 코드
│   ├── core/                  # 핵심 모듈
│   │   ├── lifecycle.py       # Agent/Project 생명주기 관리
│   │   ├── registry.py        # Agent 정의 등록소
│   │   └── trace.py           # 실행 추적 시스템
│   ├── factory/               # Agent 생성
│   │   └── agent_factory.py   # Agent 인스턴스 생성
│   ├── runtime/               # 실행 환경
│   │   ├── orchestrator.py    # 실행 조율
│   │   └── pipeline.py        # 파이프라인 정의
│   └── providers/             # LLM/Tool 추상화
│
├── agent_templates/           # Agent 정의 템플릿
│   ├── core/                  # 기본 에이전트
│   │   ├── planner.yaml
│   │   ├── executor.yaml
│   │   ├── writer.yaml
│   │   └── qa.yaml
│   └── domain/                # 도메인 에이전트
│       ├── swmm.yaml
│       ├── gis.yaml
│       └── literature.yaml
│
├── projects/                  # 프로젝트 저장소
└── prometheus/                # (레거시 - 마이그레이션 예정)
```

---

## 🚀 빠른 시작

### 설치

```bash
pip install -r requirements.txt
```

### 환경 설정

```bash
cp .env.example .env
# .env 파일에 API 키 설정
```

### 기본 사용법

```python
from nexous import AgentFactory, Orchestrator, ExecutionMode
from nexous.core import get_registry

# 1. Registry에서 사용 가능한 Agent 확인
registry = get_registry()
print(registry.list_all())

# 2. Agent 생성
factory = AgentFactory()
agents = factory.create_project_agents(
    project_id="my_project",
    agent_names=["planner", "executor", "writer", "qa"]
)

# 3. 실행
orchestrator = Orchestrator()
result = await orchestrator.execute(
    project_id="my_project",
    request="울산 태화지구 침수 분석",
    agents=agents,
    mode=ExecutionMode.PIPELINE
)

# 4. 결과 확인
print(result.results)
print(f"Trace ID: {result.trace_id}")
```

---

## 📋 플랫폼 모듈 설명

### Core - 핵심

| 모듈 | 설명 |
|------|------|
| `LifecycleManager` | Agent/Project 상태 전이 관리 |
| `AgentRegistry` | Agent 정의 등록 및 조회 |
| `Trace` / `TraceStore` | 실행 기록 추적 및 저장 |

### Factory - 생성

| 모듈 | 설명 |
|------|------|
| `AgentFactory` | Agent 인스턴스 생성 |
| `AgentInstance` | 생성된 Agent 래퍼 |

### Runtime - 실행

| 모듈 | 설명 |
|------|------|
| `Orchestrator` | Agent 실행 조율 |
| `Pipeline` | 실행 파이프라인 정의 |

---

## 🔧 Agent 템플릿 정의

Agent는 YAML 파일로 정의됩니다:

```yaml
# agent_templates/domain/swmm.yaml
name: swmm
display_name: SWMM Agent
description: EPA-SWMM 기반 침수 시뮬레이션

category: domain
role: executor

llm:
  provider: openai
  model: gpt-4o
  temperature: 0.3

tools:
  - python_exec
  - file_read

dependencies:
  - gis

system_prompt: |
  당신은 EPA-SWMM 전문가입니다...
```

---

## 📊 역할 분리 요약

| 구분 | NEXOUS (플랫폼) | ProjectAgent (생성물) |
|------|----------------|----------------------|
| 생명주기 | ✅ 관리 | - |
| 추적/감사 | ✅ 기록 | - |
| LLM 추상화 | ✅ 제공 | ✅ 사용 |
| Tool 추상화 | ✅ 제공 | ✅ 사용 |
| 분석 | ❌ | ✅ 수행 |
| 글쓰기 | ❌ | ✅ 수행 |
| 판단 | ❌ | ✅ 수행 |
| 도메인 지식 | ❌ | ✅ 보유 |

---

## 🛠️ 개발 상태

- [x] Core: Lifecycle, Registry, Trace
- [x] Factory: AgentFactory
- [x] Runtime: Orchestrator, Pipeline
- [x] Agent Templates: Core (4개), Domain (3개)
- [ ] Providers: LLM/Tool 통합
- [ ] UI: 대시보드
- [ ] Tests: 통합 테스트

---

## 📄 라이선스

MIT License

---

## 🤝 기여

NEXOUS 프로젝트에 기여해 주셔서 감사합니다.
플랫폼 관점을 유지하며 개발해 주세요!
